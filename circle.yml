## Customize the test machine
machine:

  timezone:
    America/Detroit # Set the timezone

## Customize checkout
checkout:
  post:
    - git submodule sync
    - git submodule update --init # use submodules


## Customize deployment commands
deployment:
    branch: readme-edits
      appname: ISEM501-170068

